import { StyleSheet } from 'react-native';

export const cs = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'black',
    },
    width100: {
        width: '100%',
    },
});
