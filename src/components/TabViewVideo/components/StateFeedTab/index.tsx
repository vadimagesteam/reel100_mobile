import React from 'react';
import { View, Text } from 'react-native';

const StateFeedTab = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }} >
            <Text style={{ color: '#fff' }}>State Feed</Text>
        </View>
    );
};

export default StateFeedTab;
