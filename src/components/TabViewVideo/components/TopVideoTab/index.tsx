import React from 'react';
import { View, Text } from 'react-native';

const TopVideoTab = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }} >
            <Text style={{ color: '#fff' }}>Top Video Display</Text>
        </View>
    );
};

export default TopVideoTab;
