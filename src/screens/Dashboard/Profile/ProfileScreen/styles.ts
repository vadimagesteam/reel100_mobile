import { StyleSheet } from 'react-native';

export const cs = StyleSheet.create({
    input: {
        padding: 10,
        backgroundColor: '#1b1b1b',
        marginRight: 15,
        borderRadius: 8,
    },
});
